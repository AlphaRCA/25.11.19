import 'package:flutter/animation.dart';

class AppSizes {
  static const BORDER_RADIUS = 16.0;
  static const CARD_PADDING = 24.0;
  static const CARD_MARGIN = 8.0;
  static const QUESTION_CARD_HEIGHT = 220.0;
  static const NUMBER_PICKER_ITEM_HEIGHT = 50.0;
  static const NUMBER_PICKER_ITEM_WIDTH = 60.0;
  static const BOTTOM_BUTTON_SIZE = 48.0;

  // Animations
  static const ANIMATION_DURATION = Duration(seconds: 1);
  static const PAGE_ANIMATION_DURATION = Duration(milliseconds: 400);
  static const DUAL_SCROLL_ANIMATION_DURATION = Duration(milliseconds: 500);

  static const ANIMATION_TYPE = Curves.ease;
}
